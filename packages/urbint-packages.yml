generated: '2026-09-02'
method: searched
source: https://registry.npmjs.org/-/v1/search?text=urbint (npm registry metadata, fetched
  2026-09-02)
name: Urbint packages
summary: >-
  Urbint publishes no API client SDK, because it publishes no public API. The only
  first-party packages it distributes are two npm packages under the @urbint scope,
  both published by the npm user "urbint": a React Native design-system component
  library and an unpopulated map-UI placeholder. PyPI, RubyGems, crates.io, Maven
  Central, NuGet and pkg.go.dev were checked for an "urbint" package and all returned
  404 / no result.
registries_checked:
- registry: npm
  method: search + package metadata
  result: 2 packages under the @urbint scope
- registry: pypi
  probed:
  - urbint
  - urbint-sdk
  - urbint-api
  result: 404 on all
- registry: rubygems
  probed:
  - urbint
  result: 404
- registry: hex
  probed:
  - urbint
  result: >-
    empty result set (notable: Urbint's public GitHub org is largely Elixir, but
    nothing was published to Hex)
- registry: crates.io / maven-central / nuget / pkg.go.dev
  result: no first-party package found
packages:
- name: '@urbint/cl'
  registry: npm
  url: https://www.npmjs.com/package/@urbint/cl
  metadata_url: https://registry.npmjs.org/@urbint%2Fcl
  official: true
  official_evidence: >-
    published by npm maintainer "urbint"; repository field points at
    github.com/urbint/urbint-cl; README signed "MIT (c) Urbint"
  language: TypeScript
  kind: ui-component-library
  description: >-
    Enterprise-ready React Native component library built with Unistyles 3.0 — 40+
    components, light/dark themes, iOS/Android/Web.
  version: 1.0.1-3
  published: '2026-02-10'
  first_published: '2026-01-20'
  versions_published: 4
  license: MIT
  repository: https://github.com/urbint/urbint-cl
  repository_status: >-
    404 — the linked GitHub repo is private or removed, so the package source is only
    distributable via the npm tarball
  ships:
  - >-
    llms.txt (56,801 bytes) — full component/token reference written for LLM
    consumption; captured verbatim at llms/urbint-cl-llms.txt
  - >-
    mcp-server/ — a local-stdio MCP server (@urbint/cl-mcp-server) exposing 8 tools
    and 5 resource templates; see mcp/urbint-mcp.yml
  - >-
    mcp-config.example.json — the vendor's own MCP client configuration example
  note: >-
    This is a design-system library, NOT an API client SDK. No SDKs pointer is emitted
    for it, because Urbint has no API for an SDK to wrap.
- name: '@urbint/map-ui'
  registry: npm
  url: https://www.npmjs.com/package/@urbint/map-ui
  metadata_url: https://registry.npmjs.org/@urbint%2Fmap-ui
  official: true
  official_evidence: published by npm maintainer urbint
  kind: placeholder
  description: null
  version: 0.0.1
  published: '2026-04-06'
  first_published: '2026-04-06'
  versions_published: 1
  license: null
  repository: null
  note: >-
    A single 0.0.1 publish with no README, no license and no repository, depending
    only on mapbox-gl. Reads as a scope/name reservation rather than a shipped
    library; recorded because it is genuinely first-party, not because it is usable.
sdk_count: 0
package_count: 2
